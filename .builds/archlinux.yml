image: archlinux
packages:
    - zig
tasks:
    - build: |
          cd liskvork
          zig build --summary all
    - build_all: |
          cd liskvork
          zig build -Dbuild_all --summary all
    - test: |
          cd liskvork
          zig build test --summary all
